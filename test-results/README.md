Drop hardware test results here: crash logs, screenshots, atmosphère/hbloader
error codes, anything observed when running a `switch/build/*.nro` on real
hardware. Include which `.nro` (and ideally which git commit) was tested,
since the build isn't reproducible from the file alone once the source
moves on.
