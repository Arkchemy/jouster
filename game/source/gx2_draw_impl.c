/* The single translation unit that carries the GX2 draw body.
 *
 * Everything else -- including all 200-odd files of recompiled game code --
 * gets only a declaration from cafeos_gx2_draw.h. See that header for why
 * this separation is under test as well as being the tidier arrangement.
 */
#define ARKCHEMY_GX2_DRAW_IMPL 1
#include "cafeos_gx2.h"
